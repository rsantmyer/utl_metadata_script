## Test prerequisites

These tests require:

- Oracle Database
- utPLSQL v3 installed in the database
- privileges to create/drop fixture objects
- access to the `ut` package

Run tests with:

```sql
@tests/run_all.sql
